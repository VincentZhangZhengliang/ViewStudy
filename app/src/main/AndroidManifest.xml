<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.topzrt.viewstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestActivity" />
        <activity android:name=".CwTestActivity" />
        <activity android:name=".PathTestActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".Day1Activity" />
        <activity android:name=".BasicShapeActivity" />
        <activity android:name=".activities.CanvasActivity" />
        <activity android:name=".activities.PicTextActivity" />
        <activity android:name=".activities.PathTestActivity" />
        <activity android:name=".activities.BezierActivity" />
        <activity android:name=".Day2Activity" />
        <activity android:name=".Day0Activity" />
        <activity android:name=".PropertyAnimation1Activity"></activity>
    </application>

</manifest>